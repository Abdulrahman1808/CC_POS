---
description: Push changes to git after edits
---

# Auto Push Workflow

// turbo-all

1. Stage all changes:
```powershell
git add -A
```

2. Commit with descriptive message:
```powershell
git commit -m "chore: Update files"
```

3. Push to remote:
```powershell
git push
```
