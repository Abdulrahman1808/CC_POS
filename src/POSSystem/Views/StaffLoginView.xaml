<UserControl x:Class="POSSystem.Views.StaffLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:POSSystem.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
        
        <Style x:Key="StaffButton" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="140"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#475569"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PinButton" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#475569"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#1E1E2E" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Header -->
            <TextBlock Text="👤 Who's Working?"
                       FontSize="32" FontWeight="Bold" Foreground="White"
                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="14" Foreground="#94A3B8"
                       HorizontalAlignment="Center" Margin="0,0,0,32"/>
            
            <!-- Staff Selection Grid -->
            <ItemsControl ItemsSource="{Binding StaffMembers}"
                          Visibility="{Binding ShowStaffList, Converter={StaticResource BoolToVisibility}}"
                          HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel MaxWidth="500"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource StaffButton}"
                                Command="{Binding DataContext.SelectStaffCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <StackPanel HorizontalAlignment="Center">
                                <Border Width="60" Height="60" CornerRadius="30" Background="#475569" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Name, Converter={StaticResource InitialsConverter}, FallbackValue=?}"
                                               FontSize="22" FontWeight="Bold" Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                           HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="120"/>
                                <TextBlock Text="{Binding Level}" FontSize="11" Foreground="#64748B"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- PIN Entry Panel -->
            <Border Visibility="{Binding ShowStaffList, Converter={StaticResource InverseBool}}"
                    Background="#1E293B" CornerRadius="24" Padding="40" MinWidth="360">
                <StackPanel>
                    <!-- Selected Staff Info -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                        <Border Width="48" Height="48" CornerRadius="24" Background="#6366F1" Margin="0,0,12,0">
                            <TextBlock Text="{Binding SelectedStaff.Name, Converter={StaticResource InitialsConverter}, FallbackValue=?}"
                                       FontSize="18" FontWeight="Bold" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedStaff.Name, FallbackValue=Staff}" 
                                       FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="{Binding SelectedStaff.Level, FallbackValue=Cashier}" 
                                       FontSize="12" Foreground="#64748B"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- PIN Display -->
                    <Border Background="#0F172A" CornerRadius="12" Padding="20,16" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Ellipse Width="16" Height="16" Margin="8,0" Fill="{Binding Pin.Length, Converter={StaticResource PinDotConverter}, ConverterParameter=0, FallbackValue=#475569}"/>
                            <Ellipse Width="16" Height="16" Margin="8,0" Fill="{Binding Pin.Length, Converter={StaticResource PinDotConverter}, ConverterParameter=1, FallbackValue=#475569}"/>
                            <Ellipse Width="16" Height="16" Margin="8,0" Fill="{Binding Pin.Length, Converter={StaticResource PinDotConverter}, ConverterParameter=2, FallbackValue=#475569}"/>
                            <Ellipse Width="16" Height="16" Margin="8,0" Fill="{Binding Pin.Length, Converter={StaticResource PinDotConverter}, ConverterParameter=3, FallbackValue=#475569}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Error Message -->
                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="13" Foreground="#EF4444"
                               HorizontalAlignment="Center" Margin="0,0,0,16"
                               Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"/>
                    
                    <!-- Numeric Keypad -->
                    <Grid HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Row="0" Grid.Column="0" Content="1" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="1"/>
                        <Button Grid.Row="0" Grid.Column="1" Content="2" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="2"/>
                        <Button Grid.Row="0" Grid.Column="2" Content="3" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="3"/>
                        <Button Grid.Row="1" Grid.Column="0" Content="4" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="4"/>
                        <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="5"/>
                        <Button Grid.Row="1" Grid.Column="2" Content="6" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="6"/>
                        <Button Grid.Row="2" Grid.Column="0" Content="7" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="7"/>
                        <Button Grid.Row="2" Grid.Column="1" Content="8" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="8"/>
                        <Button Grid.Row="2" Grid.Column="2" Content="9" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="9"/>
                        <Button Grid.Row="3" Grid.Column="0" Content="⌫" Style="{StaticResource PinButton}" Command="{Binding BackspaceCommand}" FontSize="22"/>
                        <Button Grid.Row="3" Grid.Column="1" Content="0" Style="{StaticResource PinButton}" Command="{Binding AppendDigitCommand}" CommandParameter="0"/>
                        <Button Grid.Row="3" Grid.Column="2" Content="✓" Style="{StaticResource PinButton}" Command="{Binding ValidatePinCommand}" Background="#10B981"/>
                    </Grid>
                    
                    <!-- Back Button -->
                    <Button Content="← Back" Style="{StaticResource ActionButton}" 
                            Background="#475569"
                            Command="{Binding BackToListCommand}"
                            HorizontalAlignment="Center" Margin="0,24,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
